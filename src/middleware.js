import { NextResponse } from "next/server";

export default function middleware(req) {

  // const role = "Admin";
  // const isLogin = false;
  // if (req.nextUrl.pathname.includes("/admin") && !isLogin) {
  //   return NextResponse.redirect(new URL("/", req.url));
  // }
  // if (req.nextUrl.pathname.includes("/user") && !isLogin) {
  //   return NextResponse.redirect(new URL("/", req.url));
  // }
}
